[build-system]
requires = ["hatchling>=1.0"]
build-backend = "hatchling.build"

[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "A service built with Forge"
dependencies = [
    "forge-kaif>=0.1.0",
]
