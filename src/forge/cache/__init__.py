"""
Caching module — decorator-based caching with pluggable backends.

Provides a @cached decorator, cache invalidation helpers, and backend-agnostic
cache operations. Ships with in-memory LRU and Redis backends.
"""
